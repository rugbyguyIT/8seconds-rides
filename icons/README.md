# App icons

GitHub API pushes are text-only, so the PNG icons aren't in this commit.
Copy `icon-192.png` and `icon-512.png` from the `rugbyguyIT/hlsr` repo's `icons/`
folder (or drop in Ride-Management-branded art at those sizes) before enabling
push notifications — the service worker references `/icons/icon-192.png`.
